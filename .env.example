# 数据库（PostgreSQL，Vercel 可用 Vercel Postgres 或外部连接串）
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Auth.js / NextAuth（必填，部署时生成或填写）
AUTH_SECRET=""
# 部署到 Vercel 后填你的站点地址，例如 https://xxx.vercel.app
AUTH_URL="https://your-app.vercel.app"

# 签证资料上传（Vercel Blob，在 Vercel 项目 → Storage 创建后自动注入）
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxx"
